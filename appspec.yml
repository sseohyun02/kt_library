version: 0.0
os: linux

files:
  - source: /
    destination: /home/ec2-user/kt_library
    overwrite: true

hooks:
  BeforeInstall:
    - location: scripts/fix_permissions.sh
      timeout: 300
      runas: root

  AfterInstall:
    - location: scripts/deploy.sh
      timeout: 1200
      runas: root
